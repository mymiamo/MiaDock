<UserControl x:Class="MiaDock.App.Views.Onboarding.AppearanceStepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <StackPanel Spacing="18" MaxWidth="620" HorizontalAlignment="Left">
    <TextBlock Text="Tema seçin" Style="{ThemeResource TitleTextBlockStyle}" />
    <ComboBox ItemsSource="{Binding Themes}" ItemTemplate="{StaticResource SettingOptionItemTemplate}"
              SelectedIndex="{Binding ThemeIndex, Mode=TwoWay}" HorizontalAlignment="Stretch"
              AutomationProperties.Name="Dock teması" />
    <TextBlock Text="Önizleme" Style="{ThemeResource SubtitleTextBlockStyle}" />
    <Border Width="292" Height="46" CornerRadius="23" Background="#FF000000"
            BorderBrush="#FF252525" BorderThickness="1" HorizontalAlignment="Left">
      <Grid Padding="14,0" ColumnSpacing="12">
        <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
        <FontIcon Glyph="&#xE8D6;" FontSize="15" Foreground="White" />
        <TextBlock Grid.Column="1" Text="MiaDock hazır" Foreground="White" VerticalAlignment="Center" />
        <FontIcon Grid.Column="2" Glyph="&#xE9D9;" FontSize="15" Foreground="White" />
      </Grid>
    </Border>
    <TextBlock Text="Tema değişikliği anında önizlenir. Kurulum iptal edilirse önceki tema geri yüklenir."
               TextWrapping="Wrap" Opacity="0.68" />
  </StackPanel>
</UserControl>
