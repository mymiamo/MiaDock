<UserControl x:Class="MiaDock.App.Views.Onboarding.FullscreenStepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <StackPanel Spacing="18" MaxWidth="620" HorizontalAlignment="Left">
    <TextBlock Text="Tam ekran davranışı" Style="{ThemeResource TitleTextBlockStyle}" />
    <ToggleSwitch Header="Tam ekranda medya olaylarını göster"
                  IsOn="{Binding FullscreenEnabled, Mode=TwoWay}"
                  AutomationProperties.Name="Tam ekran bildirimleri" />
    <TextBlock Text="Bildirim görünümü" Style="{ThemeResource SubtitleTextBlockStyle}" />
    <ComboBox ItemsSource="{Binding FullscreenStyles}" ItemTemplate="{StaticResource SettingOptionItemTemplate}"
              SelectedIndex="{Binding FullscreenStyleIndex, Mode=TwoWay}" HorizontalAlignment="Stretch"
              AutomationProperties.Name="Tam ekran bildirim görünümü" />
    <TextBlock Text="Normal dock tam ekran sırasında gizlenir. Seçilen olaylar varsayılan olarak beş saniye görünür."
               TextWrapping="Wrap" Opacity="0.68" />
  </StackPanel>
</UserControl>
