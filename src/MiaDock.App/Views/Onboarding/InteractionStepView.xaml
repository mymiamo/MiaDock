<UserControl x:Class="MiaDock.App.Views.Onboarding.InteractionStepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <StackPanel Spacing="18" MaxWidth="620" HorizontalAlignment="Left">
    <TextBlock Text="Etkileşim davranışı" Style="{ThemeResource TitleTextBlockStyle}" />
    <TextBlock Text="Dock'un ne zaman genişleyeceğini seçin. Klavye ve tıklama erişimi her durumda kullanılabilir."
               TextWrapping="Wrap" />
    <ComboBox ItemsSource="{Binding InteractionModes}" ItemTemplate="{StaticResource SettingOptionItemTemplate}"
              SelectedIndex="{Binding InteractionModeIndex, Mode=TwoWay}" HorizontalAlignment="Stretch"
              AutomationProperties.Name="Dock etkileşim davranışı" />
    <InfoBar IsOpen="True" Severity="Informational" Title="Odak davranışı"
             Message="Hover ile açılırken MiaDock aktif uygulamanın klavye odağını almaz." />
  </StackPanel>
</UserControl>
