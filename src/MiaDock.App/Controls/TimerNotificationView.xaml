<UserControl x:Class="MiaDock.App.Controls.TimerNotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Style="{StaticResource DockNotificationLayoutStyle}"
        AutomationProperties.Name="Süre doldu"
        AutomationProperties.LiveSetting="Assertive">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto" />
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="Auto" />
    </Grid.ColumnDefinitions>
    <Border Style="{StaticResource DockNotificationIconContainerStyle}">
      <FontIcon Glyph="&#xE823;" Style="{StaticResource DockModuleIconStyle}" />
    </Border>
    <StackPanel Grid.Column="1" VerticalAlignment="Center">
      <TextBlock Text="Süre doldu" Style="{StaticResource DockTitleTextStyle}" />
      <TextBlock Text="Zamanlayıcı tamamlandı" Style="{StaticResource DockCaptionTextStyle}" />
    </StackPanel>
    <Button x:Name="SilenceAlarmButton"
            Grid.Column="2"
            MinWidth="108"
            Height="44"
            Padding="10,0"
            IsTabStop="True"
            Style="{StaticResource DockInlineButtonStyle}"
            Command="{Binding CompactSecondaryCommand}"
            ToolTipService.ToolTip="{Binding CompactSecondaryText}"
            AutomationProperties.Name="{Binding CompactSecondaryText}"
            AutomationProperties.HelpText="{Binding CompactSecondaryText}">
      <StackPanel Orientation="Horizontal" Spacing="6">
        <FontIcon Glyph="&#xE7ED;" FontSize="13" />
      <TextBlock Text="{Binding CompactSecondaryText}"
                 VerticalAlignment="Center"
                 Style="{StaticResource DockBodyTextStyle}" />
      </StackPanel>
    </Button>
  </Grid>
</UserControl>
