<UserControl
    x:Class="MiaDock.App.Controls.CollapsedIslandView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <Grid Padding="{StaticResource IslandCompactPadding}" ColumnSpacing="9">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto" />
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="Auto" />
    </Grid.ColumnDefinitions>
    <FontIcon Glyph="&#xE8D6;"
              FontSize="15"
              Foreground="{ThemeResource IslandStyleAccentBrush}"
              AutomationProperties.AccessibilityView="Raw" />
    <TextBlock Grid.Column="1"
               VerticalAlignment="Center"
               MaxLines="1"
               Style="{StaticResource IslandBodyTextStyle}"
               Text="{Binding Current.Track.Title}" />
    <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="2" VerticalAlignment="Center">
      <Rectangle Width="3" Height="8" RadiusX="1.5" RadiusY="1.5" Fill="{ThemeResource IslandStyleAccentBrush}" />
      <Rectangle Width="3" Height="14" RadiusX="1.5" RadiusY="1.5" Fill="{ThemeResource IslandStyleAccentBrush}" />
      <Rectangle Width="3" Height="10" RadiusX="1.5" RadiusY="1.5" Fill="{ThemeResource IslandStyleAccentBrush}" />
    </StackPanel>
  </Grid>
</UserControl>
