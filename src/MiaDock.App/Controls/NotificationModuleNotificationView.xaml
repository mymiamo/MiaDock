<UserControl x:Class="MiaDock.App.Controls.NotificationModuleNotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Padding="18,12" ColumnSpacing="13">
    <Grid.ColumnDefinitions><ColumnDefinition Width="34" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
    <Border Width="32" Height="32" CornerRadius="9" Background="{ThemeResource IslandStyleAccentBrush}" VerticalAlignment="Center">
      <FontIcon Glyph="{Binding LeadingGlyph}" FontSize="16" Foreground="Black" />
    </Border>
    <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="1">
      <TextBlock Text="{Binding PrimaryText}" FontSize="11" Opacity="0.66" MaxLines="1" TextTrimming="CharacterEllipsis" />
      <TextBlock Text="{Binding SecondaryText}" Style="{StaticResource IslandTitleTextStyle}" MaxLines="1" TextTrimming="CharacterEllipsis" />
      <TextBlock Text="{Binding ValueText}" Style="{StaticResource IslandCaptionTextStyle}" MaxLines="1" TextTrimming="CharacterEllipsis" />
    </StackPanel>
  </Grid>
</UserControl>
