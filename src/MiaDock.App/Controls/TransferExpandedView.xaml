<UserControl x:Class="MiaDock.App.Controls.TransferExpandedView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Style="{StaticResource DockExpandedModuleLayoutStyle}"
        AutomationProperties.Name="{Binding Title}">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <Border Style="{StaticResource DockSectionStyle}">
      <Grid ColumnSpacing="12" RowSpacing="10">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="44" />
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Border Style="{StaticResource DockModuleIconContainerStyle}">
          <FontIcon Glyph="&#xE896;" Style="{StaticResource DockModuleIconStyle}" />
        </Border>
        <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
          <TextBlock Text="{Binding Title}" MaxLines="1" TextTrimming="CharacterEllipsis"
                     Style="{StaticResource DockTitleTextStyle}"
                     ToolTipService.ToolTip="{Binding Title}" />
          <TextBlock Text="{Binding StatusText}"
                     MaxLines="1" TextTrimming="CharacterEllipsis"
                     Style="{StaticResource DockBodyTextStyle}"
                     ToolTipService.ToolTip="{Binding StatusText}" />
        </StackPanel>
        <TextBlock Grid.Column="2"
                   Text="{Binding ValueText}"
                   VerticalAlignment="Center"
                   Style="{StaticResource DockStatusTextStyle}" />
        <ProgressBar Grid.Row="1"
                     Grid.ColumnSpan="3"
                     Value="{Binding Progress}"
                     Maximum="1"
                     Style="{StaticResource DockProgressBarStyle}"
                     AutomationProperties.Name="Aktarım ilerlemesi" />
      </Grid>
    </Border>
    <TextBlock Grid.Row="1"
               Margin="4,0"
               Text="{Binding ActiveCountText}"
               Style="{StaticResource DockCaptionTextStyle}" />
  </Grid>
</UserControl>
