<UserControl x:Class="MiaDock.App.Controls.TransferExpandedView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Padding="22,10,22,14" RowSpacing="14">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <Grid ColumnSpacing="14">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <Border Width="48" Height="48" CornerRadius="16"
              Background="{ThemeResource IslandStyleControlBrush}">
        <FontIcon Glyph="&#xE896;" FontSize="24"
                  Foreground="{ThemeResource IslandStyleAccentBrush}" />
      </Border>
      <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="3">
        <TextBlock Text="{Binding Title}" MaxLines="1" TextTrimming="CharacterEllipsis"
                   Style="{StaticResource IslandTitleTextStyle}" />
        <TextBlock Text="{Binding StatusText}" Style="{StaticResource IslandBodyTextStyle}" />
      </StackPanel>
      <TextBlock Grid.Column="2" Text="{Binding ValueText}" VerticalAlignment="Center"
                 Style="{StaticResource IslandCaptionTextStyle}" />
    </Grid>
    <ProgressBar Grid.Row="1" Value="{Binding Progress}" Maximum="1" Height="5"
                 AutomationProperties.Name="Aktarım ilerlemesi" />
    <TextBlock Grid.Row="2" Text="{Binding ActiveCountText}"
               Style="{StaticResource IslandCaptionTextStyle}" />
  </Grid>
</UserControl>
